# Shareware archive cache

`tools/fetch_shareware.sh` downloads `huntbgin-box.zip` here. ZIP files are
ignored by Git because the build can reproduce them from the configured source.
You may also place an already-downloaded archive at:

`vendor/shareware/huntbgin-box.zip`
